{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "ProductProfile",
  "description": "A stock product profile provides a summary of products' key characteristics",
  "type": "object",
  "properties": {
    "doc_type": {
      "type": "string",
      "pattern": "^product-profile$"
    },
    "version": {
      "type": "string",
      "format": "semver"
    },
    "_id": {
      "type": "string",
      "minLength": 1
    },
    "active": {
      "type": "boolean"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "productCategory": {
      "type": "string",
      "description": "Id of the product category, this product profile belongs to."
    },
    "description": {
      "type": "string",
      "description": "used to add extra details about a product profile."
    },
    "productType": {
      "type": "string",
      "description": "Id of the product type, this product profile belongs to."
    },
    "productPresentation": {
      "type": "string",
      "description": "Id of the product presentation, this product profile belongs to."
    }
  },
  "required": [
    "doc_type",
    "name",
    "product",
    "category",
    "_id"
  ]
}

